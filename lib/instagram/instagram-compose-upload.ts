/**
 * TODO: Integrate with your storage (e.g. Supabase Storage) and return a permanent HTTPS URL
 * for persisting on `content_items.thumbnail` / `content_attachments`.
 */
export async function uploadInstagramComposeMedia(_file: File): Promise<string | null> {
  void _file
  return null
}
